server:
  port: 8080
#  servlet:
#    context-path: /api
logging:
  level:
    root: info
    com.hsu: debug
  file: logs/debug.log      

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/apiplatform?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
  jpa:    
    hibernate:
      ddl-auto: update
    show-sql: true